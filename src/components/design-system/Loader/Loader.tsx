import { LoopIcon } from '../';
import './loader.sass';

const Loader = () => {
    return (
        <div className="loader">
            <LoopIcon />
        </div>
    );
};

export default Loader;
